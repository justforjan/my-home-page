justforjan.eu {
	root * /srv
	file_server
	try_files {path} /index.html
}

api.justforjan.eu {
    handle_path /images/* {
        reverse_proxy minio:9000 {
            uri {path}  # Ensures the full path is forwarded
        }
    }
    reverse_proxy backend:8000
}